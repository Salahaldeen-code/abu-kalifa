/**
 * Converts a number to a pixel string
 * @param value - The numeric value to convert
 * @returns A string with "px" suffix (e.g., 16 -> "16px")
 */
export function px(value: number): string {
  return `${value}px`;
}

